# run persistant paper trader
nohup python -u src/strategy/paper_runner.py --config config/paper.ec2.json >> logs/paper_runner.log 2>&1 & echo $! > logs/paper_runner.pid

# check paper trade is live
pgrep -af "src/strategy/paper_runner.py"

# kill paper trader
pkill -f "src/strategy/paper_runner.py"

# ssh
ssh -i "GTC-Low-Frequency.pem" ubuntu@ec2-13-222-173-252.compute-1.amazonaws.com
